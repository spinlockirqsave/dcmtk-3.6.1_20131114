#
#	Makefile for dcmjpls/tests
#



SHELL = /bin/sh
srcdir = .
top_srcdir = ..
configdir = ../../config

include $(configdir)/Makefile.def


all:

check:

install:

clean:
	rm -f $(TRASH)

distclean:
	rm -f $(DISTTRASH)

dependencies:
